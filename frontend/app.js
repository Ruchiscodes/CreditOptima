const form=document.querySelector('#risk-form'),result=document.querySelector('#result');
form.addEventListener('submit',async(e)=>{e.preventDefault();const button=form.querySelector('button');button.disabled=true;button.innerHTML='Assessing applicant…';const data=Object.fromEntries(new FormData(form));for(const k in data)data[k]=data[k]===''?null:Number(data[k]);try{const r=await fetch('/v1/score',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const out=await r.json();if(!r.ok)throw new Error(out.detail||'Assessment failed');const reasons=out.adverse_action_reasons.length?out.adverse_action_reasons.map(x=>`<div class="reason"><b>${x.code}</b><span>${x.description}</span></div>`).join(''):'<div class="reason"><span>No material adverse drivers detected.</span></div>';result.innerHTML=`<p class="eyebrow">ASSESSMENT / COMPLETE</p><div class="decision"><div class="scoreline"><div><div class="score">${out.credit_score}</div><div class="prob">CREDIT SCORE / 850</div></div><span class="tier">TIER ${out.risk_tier}</span></div><h3>${out.decision}</h3><div class="prob">${(out.default_probability*100).toFixed(2)}% ESTIMATED DEFAULT RISK</div><div class="reasons"><h4>PRINCIPAL RISK FACTORS</h4>${reasons}</div><div class="meta">MODEL ${out.model_version}<br>DECISION ID ${out.request_id}<br>LATENCY ${out.latency_ms} MS</div></div>`}catch(err){result.innerHTML=`<p class="eyebrow">ASSESSMENT / ERROR</p><div class="empty"><h3>Unable to score</h3><p>${err.message}</p></div>`}finally{button.disabled=false;button.innerHTML='Run risk assessment <b>→</b>'}});
